# xForge Landing

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm install
npm run build
```

If you see `'vite' is not recognized as an internal or external command` on Windows, dependencies were not installed yet (or install was done with dev/build tooling omitted). Re-run:

```bash
npm install
npm run build
```
